export type ModalType = {
  show: string;
};

export type Project = {
  num: string;
  name: string;
  url: string;
  description: string;
};

export type Skill = {
  name: string;
  category: string;
  image: string;
};
